# 📤 Інструкція: Як завантажити проект на GitHub

## Крок 1: Створіть репозиторій на GitHub

1. Відкрийте https://github.com
2. Натисніть "+" в правому верхньому куті → "New repository"
3. Назва: `embroidery-masterclasses` (або інша на ваш вибір)
4. Опис: "Веб-сайт для онлайн майстер-класів з вишивки"
5. Виберіть **Public** (щоб роботодавці могли побачити)
6. **НЕ** ставте галочки на README, .gitignore (ми вже створили)
7. Натисніть "Create repository"

## Крок 2: Підготовка локального репозиторію

Відкрийте термінал у VS Code (Ctrl + `) та виконайте команди:

```bash
# Перейдіть в папку проекту
cd "d:\Користувачі\msi\OneDrive\Унік\3 курс 1 семестр\Курсова\code"

# Ініціалізація git
git init

# Додати всі файли (окрім тих що в .gitignore)
git add .

# Перший коміт
git commit -m "Initial commit: Embroidery masterclasses website"

# Додати віддалений репозиторій (замініть YOUR_USERNAME на ваше ім'я на GitHub)
git remote add origin https://github.com/YOUR_USERNAME/embroidery-masterclasses.git

# Завантажити код на GitHub
git branch -M main
git push -u origin main
```

## Крок 3: Створіть .env файл локально

**ВАЖЛИВО**: Файл `.env` НЕ завантажується на GitHub (він в .gitignore)

Створіть файл `backend/.env` локально зі своїми даними:
```
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=ваш_реальний_пароль
DB_NAME=coffee_shop
PORT=3000
```

## Крок 4: Опціонально - GitHub Pages (для демо frontend)

Якщо хочете показати тільки frontend без backend:

1. Зайдіть в Settings репозиторію на GitHub
2. Pages (лівий sidebar)
3. Source: Deploy from a branch
4. Branch: main → папка: /frontend
5. Save

Ваш сайт буде доступний за адресою:
`https://YOUR_USERNAME.github.io/embroidery-masterclasses/`

**Примітка**: GitHub Pages показує тільки статичні файли (HTML/CSS/JS).
Backend з базою даних потрібно деплоїти окремо (Heroku, Railway, Render).

## ✅ Готово!

Тепер ваш проект на GitHub і роботодавці можуть:
- Переглянути код
- Побачити структуру проекту
- Оцінити ваші навички

## 💡 Поради для портфоліо

1. **Добре README.md** - опис проекту, скріншоти, інструкції
2. **Якісний код** - коментарі, структура
3. **Регулярні коміти** - показує процес роботи
4. **Опис технологій** - що використовували
5. **Live Demo** - якщо можливо, посилання на робочий сайт

## 🔄 Як оновлювати проект

Коли робите зміни в коді:
```bash
git add .
git commit -m "Опис змін"
git push
```
